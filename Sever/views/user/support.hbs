<body>
    <h2>Liên hệ Hỗ trợ</h2>
    <form action="/submit" method="post" id="supportForm">
        <!-- Trường input cho email -->
        <label for="email">Địa chỉ Email:</label>
        <input type="email" id="email" name="email" required><br> 
        
        <label for="message">Nội dung:</label>
        <textarea id="message" name="message" rows="4" cols="50" required></textarea><br>

        <label for="contact">Liên hệ:</label>
        <input type="tel" id="contact" name="contact" required><br>

        <button type="submit">Gửi yêu cầu</button>
    </form>

    <script>
        // JavaScript để tự động điền email nếu có
        document.addEventListener('DOMContentLoaded', function () {
  const emailInput = document.getElementById('email');
  const nameInput = document.getElementById('name');

  // Kiểm tra xem trường input có sẵn giá trị không
  if (emailInput.value.trim() === '') {
    emailInput.readOnly = false; // Cho phép người dùng điền vào trường email
  }

  // Nếu bạn có sẵn giá trị mặc định cho email, bạn có thể kiểm tra và điền giá trị này vào trường email
  // const userEmail = 'lechinhbao@gmail.com'; // Thay đổi thành email người dùng thực tế
  // if (userEmail) {
  //   emailInput.value = userEmail;
  //   emailInput.readOnly = true; // Đặt trường email thành chỉ đọc nếu đã có email
  // }
});

// ...

// Nội dung email
const mailOptions = {
  from: email, // Sử dụng địa chỉ email của người gửi
  to: 'baolcps21320@fpt.edu.vn',
  subject: `Yêu cầu Hỗ trợ từ ${name} - ${email}`, // Thêm tên người gửi vào tiêu đề
  text: `Nội dung: ${message}\nLiên hệ: ${contact}`,
};
    </script>
</body>